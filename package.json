{
	"scripts": {
		"compose": "docker-compose -f deploy/docker-compose/dev.yaml up --build",
		"compose:prod": "docker-compose -f deploy/docker-compose/prod.yaml up --build"
	},
	"devDependencies": {
		"grpcc": "^1.1.3"
	}
}
